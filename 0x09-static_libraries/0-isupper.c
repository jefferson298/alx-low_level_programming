#include "main.h"
/**
 * _isupper - Block alphabets
 * @c: The character to be inputed
 *
 * Return: 0 (success) or 1 (success)
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
