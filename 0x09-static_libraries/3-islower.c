#include "main.h"

/**
 * _islower - checks for lowercase characters
 * @c: This is the character to be checked
 * Return: 1 is returned if c is lowercased letter else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
