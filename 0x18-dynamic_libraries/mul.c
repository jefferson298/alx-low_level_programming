#include <stdio.h>

/**
 * mul - multiplies numbers
 * @a: ist multipliable integer
 * @b: 2nd multipliable integer
 * return: themultiplication of numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
