#include "main.h"
/**
 * mul -  a function that multiplies two integers
 *
 * @a: a is a Variable
 * @b: b is a Variable
 *
 * Return: 0 (success)
 */
int mul(int a, int b)
{
	int mul;
	
	mul = a * b;
	return (mul);
}
